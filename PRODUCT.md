# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

现有代码库已定栈：Vue 3 + Vite + Element Plus（前端）、FastAPI + PostgreSQL（后端，已跑通）。不换栈。

## Users

- 主要用户：AI 应用团队的 Prompt 负责人 / AI 产品经理 / 运营（管理 Prompt 资产、验证质量、持续优化）。
- 演示场景：本项目是求职作品集（AI 产品经理/运营岗），面试官会打开仓库、操作界面、追问设计决策。界面必须能证明"产品逻辑+数据驱动"能力。

## Product Purpose

给 AI 应用团队一个 Prompt 质量管理工作台：把"写完就用"的散落 Prompt 变成可版本化、可批量测试、可 AI 评分、可持续优化的资产。成功 = 用户能完成"管理 → 测试 → 批跑 → 评分 → 优化 → 沉淀版本"的完整闭环，并导出评测报告作为交付证据。

## Positioning

核心机制：Prompt 内容只存版本表（prompts 主表无 content，靠 current_version_id 指针），改 Prompt = 加新版本不动主表，可版本化、可回滚。配套：测试单元按"版本 × 模型 × 参数 × 样本变量"组合展开，AI 从 4 个维度（准确性/完整性/清晰度/稳定性）评分并给出可执行的优化建议（改写/参数/模型/验证计划）。

## Operating Context

- 本地运行：后端 uvicorn :8000，前端 vite :5173（代理 /api → 8000），PG 容器 :15432，Redis :6379。
- 默认评测模型：DeepSeek（已注册 provider + 模型）。
- 已内置 6 个中文示例 Prompt 模板（客服优化/信息抽取/长文摘要/文案改写/意图分类/数据分析），带 {user_input} 占位符。
- 数据闭环已真实跑通（真实 DeepSeek 调用产出评分与优化建议）。
- 已砍旧版单次测试体系（test_runs/results/metrics + Celery），只保留 prompt_test_* 新引擎。

## Capabilities and Constraints

功能（保持完整，重做只换视觉世界，不砍功能）：
- Prompt 资产管理：分类、Prompt CRUD、版本迭代/切换、标签、删除
- 测试任务：配置任务 → 测试单元（版本×模型×参数×样本组合）→ 实验执行（真实 LLM 调用）
- AI 评分：4 维评分 + 理由，任务级汇总（平均分/标准差/维度统计）
- 优化建议：改写建议 + 参数建议 + 模型建议 + 验证计划，可沉淀为新版本
- 报告导出：任务评测报告 HTML（打印为 PDF）+ 单元 CSV
- LLM 管理：provider CRUD、模型注册（含单价）、用量日志
- 系统设置：KV 配置（默认评测模型等）
- 项目信息页

技术约束：
- Element Plus 组件库（可用，但视觉世界可以重定义其主题）
- 中文为主，i18n 中英双语已就位
- 前端 13 个页面组件已存在，路由已清理旧版

## Brand Commitments

- 项目名 PromptWorks，中文界面。
- 无已定视觉资产（原项目是默认 Element Plus 后台风格，作为反参考）。

## Evidence on Hand

- 真实运行数据：客服 Prompt 评测 85 分（准确 90/完整 80/清晰 90/稳定 80）、优化建议真实生成、v2 版本已沉淀（数据库有记录，可演示）。
- 6 个中文模板 + 示例数据（默认分类、标签）已入库。
- docs/数据字典.md（产品视角版）、docs/复刻方案.md 在仓库内。
- 缺：用户自建评测样本集（预期后续由用户补）。

## Product Principles

1. 数据驱动：每个功能都要能产生或展示真实数据（评分、对比、报告）。
2. 闭环完整：管理→测试→评分→优化→沉淀→导出，任何一步都不能断。
3. 人机协同：AI 做诊断和提案，人做决策（优化建议是草稿，确认后沉淀版本）。
4. 可演示性：面试打开即用，6 模板 + 示例数据 + 真实评测结果说话。
5. 克制精确：Operate 模式，表达不能压过任务可用性。

## Accessibility & Inclusion

无产品特有硬性要求；沿用原项目已有的基础可访问性（focus-visible、对比度等），在重做时不回退。

<!-- 推断标注：用户画像（AI 团队 Prompt 负责人）与演示场景（求职作品集）来自复刻方案文档与用户求职背景；功能清单来自已跑通的后端 API 与前端路由；品牌承诺中"无已定视觉资产"来自对原前端默认样式的观察。 -->
